export const $ = (s, root=document) => root.querySelector(s);
export const $$ = (s, root=document) => [...root.querySelectorAll(s)];
export const esc = (v="") => String(v).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
export const uid = () => crypto.randomUUID ? crypto.randomUUID() : `${Date.now()}-${Math.random().toString(36).slice(2)}`;
export const fmtDate = v => { if(!v) return "—"; const d=new Date(v); return Number.isNaN(d.getTime())?"—":d.toLocaleString(); };
export const dateOnly = v => { if(!v) return "—"; const d=new Date(v); return Number.isNaN(d.getTime())?"—":d.toLocaleDateString(); };
export const daysLeft = v => { if(!v || v==="lifetime") return "Lifetime"; const n=Math.ceil((new Date(v)-Date.now())/86400000); return n<0?"Expired":`${n} day${n===1?"":"s"}`; };
export const toast = (message,type="success") => { const r=$("#toastRoot"); if(!r) return; const e=document.createElement("div"); e.className=`toast ${type}`; e.textContent=message; r.appendChild(e); setTimeout(()=>e.remove(),3500); };
export const confirmAction = (title,body) => new Promise(resolve=>{ const root=$("#modalRoot"); root.innerHTML=`<div class="modal-backdrop"><div class="modal"><div class="modal-head"><strong>${esc(title)}</strong></div><div class="modal-body">${esc(body)}</div><div class="modal-foot"><button class="btn ghost" data-cancel>Cancel</button><button class="btn danger" data-ok>Confirm</button></div></div></div>`; root.querySelector("[data-cancel]").onclick=()=>{root.innerHTML="";resolve(false)}; root.querySelector("[data-ok]").onclick=()=>{root.innerHTML="";resolve(true)}; });
export const modal = (title, body, onSubmit, submitText="Save") => { const root=$("#modalRoot"); root.innerHTML=`<div class="modal-backdrop"><div class="modal"><div class="modal-head"><strong>${esc(title)}</strong><button class="icon-btn" data-close>×</button></div><div class="modal-body">${body}</div><div class="modal-foot"><button class="btn ghost" data-close>Cancel</button><button class="btn primary" data-submit>${esc(submitText)}</button></div></div></div>`; root.querySelectorAll("[data-close]").forEach(b=>b.onclick=()=>root.innerHTML=""); root.querySelector("[data-submit]").onclick=async()=>{const ok=await onSubmit(root.querySelector(".modal")); if(ok!==false) root.innerHTML=""}; return root; };
export const randomKey = (prefix="AUTH", length=20) => { const chars="ABCDEFGHJKLMNPQRSTUVWXYZ23456789"; let s=""; const bytes=new Uint8Array(length); crypto.getRandomValues(bytes); for(const b of bytes)s+=chars[b%chars.length]; return `${prefix}-${s.match(/.{1,5}/g).join("-")}`; };
export const csv = rows => rows.map(r=>r.map(v=>`"${String(v??"").replaceAll('"','""')}"`).join(",")).join("\n");
export const download = (name,text,type="text/plain") => { const a=document.createElement("a"); a.href=URL.createObjectURL(new Blob([text],{type})); a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500); };
export const debounce = (fn,ms=250) => { let t; return (...a)=>{clearTimeout(t);t=setTimeout(()=>fn(...a),ms)}; };