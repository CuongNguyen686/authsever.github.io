import { store } from "./api.js";
import { $, esc, modal, confirmAction, toast } from "./utils.js";
export async function renderTweaks(v){const rows=await store.list("tweaks");v.innerHTML=`<div class="page-head"><div><h1>Tweaks</h1><p class="muted">Manage tweak identifiers, versions and iOS compatibility.</p></div><button class="btn primary" id="create">Create Tweak</button></div><div class="section-card"><div class="table-wrap"><table class="data-table"><thead><tr><th>Name</th><th>Identifier</th><th>Package</th><th>Version</th><th>Min iOS</th><th>Max iOS</th><th>Status</th><th>Description</th><th>Actions</th></tr></thead><tbody>${rows.length?rows.map(x=>`<tr><td>${esc(x.name||"")}</td><td>${esc(x.identifier||"")}</td><td>${esc(x.package||"")}</td><td>${esc(x.version||"")}</td><td>${esc(x.minIOS||"—")}</td><td>${esc(x.maxIOS||"—")}</td><td>${esc(x.status||"Enabled")}</td><td>${esc(x.description||"")}</td><td><button class="btn" data-edit="${esc(x.id)}">Edit</button> <button class="btn danger" data-del="${esc(x.id)}">Delete</button></td></tr>`).join(""):`<tr><td colspan="9"><div class="empty">No tweaks found.</div></td></tr>`}</tbody></table></div></div>`;$("#create").onclick=()=>form(v);v.querySelectorAll("[data-edit]").forEach(b=>b.onclick=()=>form(v,rows.find(x=>x.id===b.dataset.edit)));v.querySelectorAll("[data-del]").forEach(b=>b.onclick=async()=>{if(await confirmAction("Delete tweak","Delete this tweak?")){await store.remove("tweaks",b.dataset.del);toast("Tweak deleted");renderTweaks(v)}})}
function form(v,x={}){modal(x.id?"Edit Tweak":"Create Tweak",`<form id="f" class="form-grid"><div class="field"><label>Tweak Name</label><input name="name" required value="${esc(x.name||"")}"></div><div class="field"><label>Identifier</label><input name="identifier" required value="${esc(x.identifier||"")}"></div><div class="field"><label>Package</label><input name="package" value="${esc(x.package||"")}"></div><div class="field"><label>Version</label><input name="version" value="${esc(x.version||"")}"></div><div class="field"><label>Minimum iOS</label><input name="minIOS" value="${esc(x.minIOS||"")}"></div><div class="field"><label>Maximum iOS</label><input name="maxIOS" value="${esc(x.maxIOS||"")}"></div><div class="field"><label>Status</label><select name="status"><option>Enabled</option><option ${x.status==="Disabled"?"selected":""}>Disabled</option></select></div><div class="field wide"><label>Description</label><textarea name="description">${esc(x.description||"")}</textarea></div></form>`,async root=>{const p=Object.fromEntries(new FormData(root.querySelector("#f")));if(x.id)await store.update("tweaks",x.id,p);else await store.create("tweaks",p);toast("Tweak saved");renderTweaks(v)})}