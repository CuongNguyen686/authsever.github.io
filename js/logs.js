import{store}from"./api.js";import{esc,fmtDate,confirmAction,toast}from"./utils.js";
export async function renderLogs(v){const rows=await store.list("logs");v.innerHTML=`<div class="page-head"><div><h1>Logs</h1><p class="muted">Audit trail returned by the API/database.</p></div><button class="btn danger" id="clear">Clear Logs</button></div><div class="section-card"><div class="table-wrap"><table class="data-table"><thead><tr><th>Time</th><th>Action</th><th>Key</th><th>Device</th><th>IP</th><th>Package</th><th>Result</th></tr></thead><tbody>${rows.length?rows.sort((a,b)=>new Date(b.time||b.createdAt)-new Date(a.time||a.createdAt)).map(x=>`<tr><td>${fmtDate(x.time||x.createdAt)}</td><td>${esc(x.action||"")}</td><td>${esc(x.key||"—")}</td><td>${esc(x.deviceId||"—")}</td><td>${esc(x.ip||"—")}</td><td>${esc(x.package||"—")}</td><td>${esc(x.result||"")}</td></tr>`).join(""):`<tr><td colspan="7"><div class="empty">No logs found.</div></td></tr>`}</tbody></table></div></div>`;$("#clear").onclick=async()=>{if(await confirmAction("Clear logs","This removes the audit log records.")){for(const x of rows)await store.remove("logs",x.id);toast("Logs cleared");renderLogs(v)}}}