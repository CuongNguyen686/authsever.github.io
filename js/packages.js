import{store}from"./api.js";import{esc,modal,confirmAction,toast}from"./utils.js";
export async function renderPackages(v){const rows=await store.list("packages");v.innerHTML=`<div class="page-head"><div><h1>Packages</h1><p class="muted">Manage supported package identifiers and versions.</p></div><button class="btn primary" id="create">Create Package</button></div><div class="section-card"><div class="table-wrap"><table class="data-table"><thead><tr><th>Package ID</th><th>Name</th><th>Version</th><th>Min</th><th>Max</th><th>Status</th><th>Description</th><th>Actions</th></tr></thead><tbody>${rows.length?rows.map(x=>`<tr><td>${esc(x.packageId||x.id)}</td><td>${esc(x.name||"")}</td><td>${esc(x.version||"")}</td><td>${esc(x.minVersion||"—")}</td><td>${esc(x.maxVersion||"—")}</td><td>${esc(x.status||"Enabled")}</td><td>${esc(x.description||"")}</td><td><button class="btn" data-edit="${esc(x.id)}">Edit</button> <button class="btn danger" data-del="${esc(x.id)}">Delete</button></td></tr>`).join(""):`<tr><td colspan="8"><div class="empty">No packages found.</div></td></tr>`}</tbody></table></div></div>`;$("#create").onclick=()=>form(v);v.querySelectorAll("[data-edit]").forEach(b=>b.onclick=()=>form(v,rows.find(x=>x.id===b.dataset.edit)));v.querySelectorAll("[data-del]").forEach(b=>b.onclick=async()=>{if(await confirmAction("Delete package","Delete this package?")){await store.remove("packages",b.dataset.del);toast("Package deleted");renderPackages(v)}})}
function form(v,x={}){modal(x.id?"Edit Package":"Create Package",`<form id="f" class="form-grid"><div class="field"><label>Package ID</label><input name="packageId" required value="${esc(x.packageId||"")}"></div><div class="field"><label>Name</label><input name="name" value="${esc(x.name||"")}"></div><div class="field"><label>Version</label><input name="version" value="${esc(x.version||"")}"></div><div class="field"><label>Minimum Version</label><input name="minVersion" value="${esc(x.minVersion||"")}"></div><div class="field"><label>Maximum Version</label><input name="maxVersion" value="${esc(x.maxVersion||"")}"></div><div class="field"><label>Status</label><select name="status"><option>Enabled</option><option ${x.status==="Disabled"?"selected":""}>Disabled</option></select></div><div class="field wide"><label>Description</label><textarea name="description">${esc(x.description||"")}</textarea></div></form>`,async root=>{const f=new FormData(root.querySelector("#f"));const p=Object.fromEntries(f);if(x.id)await store.update("packages",x.id,p);else await store.create("packages",p);toast("Package saved");renderPackages(v)})}